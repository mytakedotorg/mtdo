import * as React from "react";

const TimelinePreviewLoadingView: React.StatelessComponent<{}> = props => (
  <div>
    <p>Loading TimelinePreview</p>
  </div>
);

export default TimelinePreviewLoadingView;
